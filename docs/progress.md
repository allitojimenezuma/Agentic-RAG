# Progress

<!-- One line per task. Status markers: PENDING | RUNNING | COMPLETED | BLOCKED -->

- [x] [COMPLETED] T1: Unblock tests: restore `find_inbound_links`+`extract_concepts` in `tools/lint_tools.py` as self-contained implementations; sync `recursion_limit` to 30 everywhere; register `path_guard_middleware` in `agents/factory.py` `tools/lint_tools.py` `config.py` `agents/factory.py`
- [x] [COMPLETED] T2: Build source-of-truth `Wiki` model (`load_wiki`, `Page`, `Section`) from `list_pages`+frontmatter+headings/links `src/agentic_rag/wiki/model.py`
- [x] [COMPLETED] T3: BM25 `wiki.search` over curated page fields + bounded depth-1 link expansion; add `rank_bm25` dep `src/agentic_rag/wiki/search.py` `pyproject.toml`
- [x] [COMPLETED] T4: `regenerate_index` from `Wiki` (no raw-H1 summaries); regenerate live `wiki/index.md` `src/agentic_rag/wiki/dedupe_index.py`
- [x] [COMPLETED] T5: Nav tools `wiki_search`/`wiki_read_page`/`wiki_summary`/`wiki_link_graph` over the model+search `src/agentic_rag/tools/nav.py`
- [x] [COMPLETED] T6: `QueryAnswer`/`SourceCitation` schema + `NavCapture` + `submit_query_answer` cite-or-die tool + pure `validate_citations` `src/agentic_rag/schemas/query.py` `src/agentic_rag/tools/grounding.py`
- [x] [COMPLETED] T7: Rebuild query agent on nav tools + `submit_query_answer`; update `build_query_prompt`; render `QueryAnswer` in `query` CLI with back-compat fallback `src/agentic_rag/agents/query.py` `src/agentic_rag/agents/prompts.py` `src/agentic_rag/cli.py`
- [x] [COMPLETED] T8: Deterministic `health_check`+`LintReport`+`schemas/lint.py`; restructure lint agent (`run_health_check` tool, `write_lint_report` accepts model|str); lint aliases delegate to model `src/agentic_rag/lint/health.py` `src/agentic_rag/schemas/lint.py` `src/agentic_rag/agents/lint.py` `src/agentic_rag/tools/lint_tools.py` `src/agentic_rag/agents/prompts.py`

<!-- Pass B — Ingest compilation + fix-agent refactor + eval harness (spec: Pass B section) -->

- [x] [COMPLETED] T9: `submit_extraction` finalization tool (pure, mirrors `submit_query_answer`) reused `ExtractionResult`/`Entity`/`Concept`/`Contradiction` + `chunk_by_heading` source chunker `src/agentic_rag/tools/ingest_grounding.py` `src/agentic_rag/io/chunker.py`
- [x] [COMPLETED] T10: Deterministic `match_page(wiki, name, page_type)` (exact slug → BM25 similar → none → conflict) + `@tool match_page` `src/agentic_rag/wiki/match.py`
- [x] [COMPLETED] T11: Migrate ingest agent off `update_index`/`read_index`/`search_index`/`find_relevant_pages`; onto `submit_extraction`/`match_page`/`wiki_read_page`/`regenerate_index`; rewrite `build_ingest_prompt` `src/agentic_rag/agents/ingest.py` `src/agentic_rag/agents/prompts.py`
- [ ] [PENDING] T12: Fix tools: add `add_frontmatter`/`fix_link`/`append_related_section`/`regenerate_index` tool in nav; remove `execute_command`/`remove_index_entry`; add new tools to guardrail write-tools set `src/agentic_rag/tools/fix_tools.py` `src/agentic_rag/tools/nav.py` `src/agentic_rag/middleware/guardrails.py`
- [ ] [PENDING] T13: Fix agent + CLI consume structured `LintReport` from `health_check`; rewrite `build_fix_prompt` (kind→tool map); CLI `fix` runs health_check, passes issues, drops shell HITL `src/agentic_rag/agents/fix.py` `src/agentic_rag/agents/prompts.py` `src/agentic_rag/cli.py`
- [ ] [PENDING] T14: Eval harness — recall@k on `wiki.search` (curated ~15 live-wiki queries, no LLM) + hallucination gate on `validate_citations` (fabricated citation dropped) `tests/eval/test_search_recall.py` `tests/eval/test_grounding_gate.py`

- T3 exports: `search(wiki: Wiki, query: str, *, k=8, types=None, tags=None, expand_links=True, depth=1) -> list[SearchHit]` in `src/agentic_rag/wiki/search.py`; `SearchHit(slug, score, sections, matched_via)` (pydantic).
- T3 note: BM25 doc = title + tags + headings + section texts (intro text = inferred summary); tokenization NFKD→ASCII→lower→split non-alphanumeric; `types`/`tags` filter candidates BEFORE BM25; direct hits skip zero-token-overlap pages; expansion = min(2,k) per source page, total cap k, EXPAND_SCORE=0.1 fixed, `matched_via='expand-link'`; empty inputs → [].
- T3 note: dep added as `rank-bm25>=0.0.5` (uv-normalized name) in pyproject.toml + uv.lock (installed 0.2.2).
- T3 note: `matched_via` values: "title" / "tags" / "section:<heading>" / "body" (first wins); `sections` = headings of matched body sections.
- T4 exports: `regenerate_index(wiki_path: Path) -> Path` in `src/agentic_rag/wiki/dedupe_index.py` — atomic rewrite of `wiki/index.md` from `load_wiki`, returns the index path. Excludes `lint-report-*` by filename; groups by `fm.type` via `_category_for_type`; entry format reuses `write_index`/`_format_entry`.
- T4 note: summaries = first-section text, `[[link]]`/markdown-stripped, single-line, ≤160 chars word-boundary truncation with "…", fallback `fm.title` — never a raw H1. Deterministic (byte-identical re-runs). Live wiki/index.md regenerated: 21 entries, 0 raw-H1 summaries (was ~9).
- T4 note: data quirk — `entities/alvaro-jimenez-martinez.md` declares `type: concept` in frontmatter, so it groups under Concepts (11 concepts + 10 entities). Grouping follows fm.type per spec.
- T5 exports: `wiki_search(query, k=8, types=None, tags=None) -> str`, `wiki_read_page(slug, section=None) -> str`, `wiki_summary() -> str`, `wiki_link_graph() -> str` — all @tools in `src/agentic_rag/tools/nav.py`.
- T5 note: nav reuses `get_wiki_path()` (no second global); loads `load_wiki` per call; output format: `Found N relevant: <slug> (score=X, sections: a; b)` then `- <slug> (...)` per hit, `+ linked: <slug>` for expansion hits; `types`/`tags` comma-separated strings.
- T5 note: `wiki_read_page` no-section → full raw markdown, FileNotFoundError propagates; section → only that section's text; unknown section → message with available headings. `wiki_link_graph` = moved `lint_tools.wiki_link_summary` (same output, old tool left importable — T8 rewires lint).
- T5 note: wiki_search does NOT yet record navigated slugs — T6 (grounding) will add `record_navigated` calls to nav.py (spec: nav tools record into NavCapture).
- T6 exports: `SourceCitation(slug, title, section=None)` + `QueryAnswer(answer, citations, confidence: Literal["high","medium","low"], suggestion)` in `src/agentic_rag/schemas/query.py`; exported from `schemas/__init__.py`.
- T6 exports: `NavCapture(navigated: set[str])`, `new_nav_capture() -> NavCapture` (sets module global `_NAV_CAPTURE`), `record_navigated(slugs: Iterable[str])` (NO-OP without active capture), `validate_citations(answer, navigated_slugs) -> QueryAnswer` (PURE: model_copy, never mutates/raises, WARNING on drops), `@tool submit_query_answer(answer, citations, confidence, suggestion="") -> str` (model_dump_json of cleaned QueryAnswer; never crashes) in `src/agentic_rag/tools/grounding.py`.
- T6 note: nav.py `wiki_search` records ALL returned slugs (incl. expand-link); `wiki_read_page` records the resolved slug on found paths, NOT on FileNotFoundError; lazy imports keep the module graph acyclic. Output strings unchanged (recording-only diff).
- T6 note: T7 must call `new_nav_capture()` per agent build and stash it as `agent._nav_capture`; CLI can read the validated QueryAnswer from the submit tool's ToolMessage content (JSON).
- T7: `agents/query.py::build_query_agent` now tools = [wiki_search, wiki_read_page, wiki_summary, submit_query_answer], stashes `agent._nav_capture = new_nav_capture()` per build. `prompts.py::build_query_prompt` updated (must end with submit_query_answer; unknown citations dropped; low→suggestion). `cli.py::query` renders Answer/Confidence/Citations/Suggestion from the LAST submit_query_answer ToolMessage (re-validates vs capture), falls back to raw final message when no submit call or no `_nav_capture` (fake-agent tests stay green).
- T7 note: citations MUST use canonical slugs exactly as returned by wiki_search/wiki_read_page (exact-string matching in the validator) — e.g. `entities/mlx`, never `mlx`.
- T7 tests: `tests/integration/test_query_grounded.py` (3 tests: agent-level cite-or-die, CLI grounded render, CLI compat fallback).
- T8 exports: `health_check(wiki_path: Path) -> LintReport` + `_render_report_markdown(report) -> str` in `src/agentic_rag/lint/health.py`; `Issue(slug, kind, severity, detail, action)` + `LintReport(pages_audited, issues, counts)` in `src/agentic_rag/schemas/lint.py`. 0 LLM calls; 7 kinds; lint-report-* excluded from content stats.
- T8 exports: `@tool run_health_check() -> str` ("Pages audited: N | Issues: M" + one line per issue) and `write_lint_report(report: LintReport | str) -> str` (LintReport → deterministic markdown; str → raw back-compat) in `src/agentic_rag/tools/lint_tools.py`.
- T8 note: `find_inbound_links` now delegates to `load_wiki` resolved outbound links (test output contract kept); `wiki_link_summary` delegates to `nav.wiki_link_graph`; `read_all_pages` kept importable. Lint agent tools = [run_health_check, wiki_link_graph, wiki_read_page, write_lint_report]; build_lint_prompt rewritten (run_health_check FIRST, semantic duplicate-coverage judgment, ALWAYS write report).
- T8 note: live-wiki counts — pages_audited=21, missing-frontmatter=17, missing-related=19, empty=1 (concepts/ai, 28 words), orphan/broken-link/stale/missing-index=0. Severity map: orphan/missing-index/broken-link/empty=high, missing-frontmatter=critical, missing-related/stale=medium.
- T8 tests: `tests/unit/test_health.py` (fixture wiki, 7 kinds, lint-report exclusion, determinism, 0-LLM assertion) + `tests/unit/test_guardrails.py` (path_guard_middleware direct: raw/ write blocked, benign read passes).
- T1 exports: `find_inbound_links(slug: str) -> str` in `src/agentic_rag/tools/lint_tools.py` — restored alias; self-contained via `list_pages()` + `[[link]]` regex; returns "Found N page(s)" / "...orphan". Used by T8 (keep as alias).
- T1 exports: `extract_concepts(content: str) -> str` in `src/agentic_rag/tools/lint_tools.py` — restored alias; delegates to `extract_headings`/`extract_links`; lists headings + `[[target]]` links. Used by T8 (keep as alias).
- T1 exports: `path_guard_middleware` now registered in `agents/factory.py::build_agent` (middleware order: audit_logging, path_guard, token_capture). Blocks write-tools with `raw/`, absolute, or `..` paths. T5/T6 nav-tool args (`slug`, `_source_path`) pass as reads; do not add nav tools to `write_tools`.
- T1 note: `tools/shared.py::read_wiki_page` propagates `FileNotFoundError` (try/except removed — pre-existing regression fix, approved by orchestrator). Tests assert propagation.
- T1 note: `recursion_limit` was already 30 in `config.py` (single runtime source via `settings.recursion_limit`) — no change needed.
- T2 exports: `load_wiki(wiki_path: Path) -> Wiki` in `src/agentic_rag/wiki/model.py` — source-of-truth model; 22 pages on live wiki (21 content + lint-report; index/log excluded by list_pages).
- T2 exports: `Page(slug:str, rel_path:Path, fm:Frontmatter, sections:list[Section], outbound_links:list[str], word_count:int)`, `Section(heading:str, level:int, text:str)`, `Wiki(pages:list[Page], by_slug:dict[str,Page])` in same module.
- T2 note: `outbound_links` are RESOLVED slugs (3-step resolver: exact → slugify short-name → unicode-preserving short-name; drops unresolved + self-links). `[[Málaga]]`→`entities/málaga`, `[[MLX]]`→`entities/mlx`.
- T2 note: 18/22 live pages have NO frontmatter — synthesized fm: type from dir, title from first H1, updated=mtime date, sources/tags=[]. load_wiki never raises on frontmatter-less/malformed pages.
- T2 note: sections include H1 + all headings; preamble text prepended to first section (or synthetic `heading=""` section). Frontmatter excluded from sections/links/word_count. Empty wiki dir → `Wiki([], {})`.
- T2 note: model INCLUDES `lint-report-*.md` as an unknown-type page — T8 health_check must skip lint-report pages for content stats; T4 regenerate_index excludes them.
- T2 infra: `.gitignore` line 2 changed `wiki/` → `/wiki/` (root-anchored) — the old unanchored pattern was swallowing `src/agentic_rag/wiki/`; data dir still ignored.

## Pass B Interface handoffs
- T9 exports: `@tool submit_extraction(entities: list[Entity], concepts: list[Concept], contradictions: list[Contradiction]) -> str` in `src/agentic_rag/tools/ingest_grounding.py` — builds `ExtractionResult`, returns `model_dump_json()`; PURE (no I/O, no NavCapture, never raises); reuses `schemas/extraction.py` UNCHANGED (via `agentic_rag.schemas.extraction`).
- T9 exports: `chunk_by_heading(markdown: str, max_chars: int = 4000) -> list[str]` in `src/agentic_rag/io/chunker.py` — splits on `^#{2,} ` headings (level-2+), prepends most-recent `^#{1,2} ` breadcrumb, never splits past max_chars, empty/whitespace → `[]`, 0 LLM. (Note: breadcrumb may repeat the chunk's own `## ` heading — matches pinned "most recent heading preceding" rule.)
- T10 exports: `MatchResult(decision: Literal["exact","similar","none","conflict"], slugs: list[str], detail: str)` (pydantic) + pure `match_page(wiki: Wiki, name: str, page_type: str) -> MatchResult` + `@tool match_page_tool(name, page_type) -> str` in `src/agentic_rag/wiki/match.py`. Pinned rule: slugify→by_slug key OR unique basename of nested slug = exact; else `search(k=5, types=[page_type])` direct-only (matched_via != "expand-link") → 1=similar, ≥2=conflict (top-2 slugs), 0=none; unknown page_type dir → untyped search fallback. 0 LLM. Live-wiki: "MLX"→exact entities/mlx, "Nonexistent Thing"→none.
- T11 exports: `@tool regenerate_index() -> str` in `src/agentic_rag/tools/nav.py` — calls `dedupe_index.regenerate_index(get_wiki_path())`, returns "Index regenerated." (additive-only nav.py diff).
- T11 note: ingest tool list is EXACTLY `[read_source, submit_extraction, match_page_tool, wiki_read_page, create_page, update_page, flag_contradiction, regenerate_index, append_log, delete_wiki_page]` (nav.wiki_read_page, NOT shared). HITL unchanged. `build_ingest_prompt` two modes; banned-tool words absent (update_index only in the "NEVER call" rule); Mode 2 uses match_page_tool + wiki_read_page (NOT wiki_search — not in the pinned tool list).
- T11 tests: `tests/integration/test_ingest_scripted.py` (scripted fake-model: submit_extraction → match_page_tool → create/update → regenerate_index → append_log; asserts no legacy tool calls; index.md regenerated; `prompt.count("update_index") == 1`).
- (none yet)