# Progress

<!-- One line per task. Status markers: PENDING | RUNNING | COMPLETED | BLOCKED -->

- [ ] [PENDING] T1: Build neutral eval fixtures — committed `tests/fixtures/eval_wiki/` (~20 clean AGENTS.md-conformant pages seeded from `wiki copy/`, `health_check` = zero issues; NEVER mutated by tests and never shaped to fit recall) + `tests/fixtures/eval_raw/` (`sample.md` normal source + `contradiction-source.md` conflicting with a corpus page) + `tests/fixtures/eval_broken_wiki/` (tiny wiki with pinned defects: missing-frontmatter `entities/broken-fm`, broken-link `entities/linker`, missing-related `entities/lonely`) + `tests/fixtures/eval_corpus.py` (`EVAL_WIKI_SRC`/`EVAL_RAW_SRC`/`EVAL_BROKEN_WIKI_SRC`, `CURATED_QUERIES` reusing the 15 natural-language pairs from the old `tests/eval/test_search_recall.py`, `HARD_QUERIES` ≥5 typo/synonym/cross-type variants, `RECALL_K=8`, floors 0.80/0.60, `copy_eval_wiki`/`copy_broken_wiki`, `eval_wiki` + `eval_env` fixtures) + `tests/fixtures/eval_judge.py` (calibrated: `judge_model()` from Settings, `FaithfulnessScore`/`RelevancyScore` with `Field(ge=0.0, le=1.0)`, few-shot 1.0/0.5/0.0 anchors pinned as module strings, strict JSON via response_format json_object with fallback, one corrective retry, final raise includes raw output) + `tests/fixtures/eval_hitl.py` (`auto_decide(state, choice, feedback, index, new_resolution)` mirroring cli.py decision shapes + tolerant `__interrupt__` parsing, `resume_auto(agent, config, ...)` via `Command(resume={"decisions": [...]})`, own deterministic unit tests for approve/reject/edit + parsing) + `tests/levels/conftest.py` (re-exports) + corpus self-check tests (eval_wiki health_check zero issues; broken-wiki reports exactly the seeded kinds; every ground-truth slug exists) `tests/fixtures/eval_wiki/` `tests/fixtures/eval_raw/` `tests/fixtures/eval_broken_wiki/` `tests/fixtures/eval_corpus.py` `tests/fixtures/eval_judge.py` `tests/fixtures/eval_hitl.py` `tests/levels/conftest.py`
- [ ] [PENDING] T2: Level 1 deterministic suite — `tests/levels/level1/test_schema_conformance.py` (every eval_wiki page: frontmatter required fields, slug↔filename match, type↔directory match, `## Related` present) + `test_path_guard_matrix.py` (exhaustive: all 7 write tools × {`raw/`, absolute, `..`, `./raw`, traversal `a/../b`} → ERROR string; `read_source` with raw path allowed; valid in-wiki slugs allowed) + `test_link_integrity.py` (health_check over corpus copy: no broken-link/orphan/missing-frontmatter/missing-index; every `[[link]]` target resolves via markdown_parser extraction) `tests/levels/level1/`
- [ ] [PENDING] T3: Level 2 deterministic tool-selection invariants — `tests/levels/level2/test_tool_selection.py` with `ScriptedChatModel` + `build_agent`: query only calls {wiki_search, wiki_read_page, wiki_summary} and never writes; ingest ordering `read_source` → `submit_extraction` (exactly once) → match decision → write → `regenerate_index` + `append_log` at end; lint calls `run_health_check` exactly once and before `write_lint_report`; fix respects the pinned kind→tool map (missing-frontmatter→add_frontmatter, broken-link→fix_link, missing-related→append_related_section, missing-index→regenerate_index, orphan/empty/stale→no fix tool); any scripted flow that triggers `flag_contradiction`/`delete_wiki_page` resumes via `eval_hitl.resume_auto` (never CLI input, never patch input) `tests/levels/level2/test_tool_selection.py`
- [ ] [PENDING] T4: Level 2 deterministic schemas + efficiency + state — `tests/levels/level2/test_argument_schemas.py` (invalid args return error strings never raise: create_page bad type/slug, flag_contradiction missing fields, fix_link bad targets, path-guard short-circuit shape) + `test_turn_efficiency.py` (happy-path tool-call caps: query ≤ 5, lint ≤ 4, fix ≤ 8, ingest ≤ 15) + `test_state_consistency.py` (post-ingest on tmp corpus copy: `regenerate_index` idempotent vs on-disk index.md, log.md appended, no orphan pages introduced, written pages valid frontmatter) `tests/levels/level2/test_argument_schemas.py` `tests/levels/level2/test_turn_efficiency.py` `tests/levels/level2/test_state_consistency.py`
- [ ] [PENDING] T5: Level 2 DAG trajectory layer — `tests/levels/level2/trajectory_contract.py` (pure: `TrajectoryContract` pydantic model with allowed/prerequisites edges (A→B = every B call after first A call)/required/required_any/write_tools/terminal_after/forbidden/max_calls/ends_with_interrupt + `validate_trajectory(contract, tool_names) -> TrajectoryReport(passed, violations)`; semantics: tool not in allowed = violation, missing required, order violations, terminal-after-last-write, max_calls breach) + `test_trajectory_contract.py` (deterministic validator tests: pass + every failure mode) + `test_trajectory_real_llm.py` (acceptance, skipif no key: `TRAJECTORY_TASKS` ~8 `TrajectoryContract`s per spec table — query single/multi-hop, ingest happy path + contradiction (ends_with_interrupt), lint, fix missing-frontmatter/broken-link/missing-related on broken-wiki copy; run REAL `build_*_agent(settings)` from `tests/acceptance/test_wiki_health.py` pattern over eval_env/broken-wiki tmp copies; record tool names from `result["messages"]`; `validate_trajectory`; aggregate pass ≥ 0.8; print full trajectories on failure; report tool-selection accuracy; never assert model wording) `tests/levels/level2/trajectory_contract.py` `tests/levels/level2/test_trajectory_contract.py` `tests/levels/level2/test_trajectory_real_llm.py`
- [ ] [PENDING] T6: Level 3 retrieval + deterministic faithfulness — `tests/levels/level3/test_context_recall.py` (recall@8 per query over CURATED_QUERIES + HARD_QUERIES on `eval_wiki` copy; aggregate floors 0.80/0.60; MEASURE and document real numbers in docstring; calibrate query phrasing to reach floors — natural language only, never page-title mirrors; corpus never modified for calibration) + `test_faithfulness.py` deterministic tier ALWAYS runs (every answer citation slug ∈ turn's NavCapture navigated set; every `[[link]]` in answer text ↔ citation consistency; no uncited links; build_final_answer confidence inference) `tests/levels/level3/test_context_recall.py` `tests/levels/level3/test_faithfulness.py`
- [ ] [PENDING] T7: Level 3 calibrated judges + contradiction handling — `test_faithfulness.py` judge tier (skipif no key: `judge_faithfulness` over scripted grounded vs fabricated answers on corpus contexts; asserts anchor separation grounded ≥ 0.7 / fabricated ≤ 0.4; stub-model round-trip test proves prompt→JSON→pydantic with Field bounds) + `test_answer_relevancy.py` (deterministic proxy always runs: non-empty answer, length bounds, query key-terms when appropriate; judge tier skipif: `judge_relevancy` relevant vs off-topic, anchor separation) + `test_contradiction_handling.py` (scripted flow: submit_extraction with conflicting claim → `match_page` decision `conflict` → `flag_contradiction` called with page_slug/existing_claim/new_claim/proposed_resolution → HITL via `resume_auto`: approve keeps page+log, reject discards, edit produces edited_action with proposed_resolution then approve → page updated + log entry + index consistent; NO CLI input anywhere) `tests/levels/level3/test_faithfulness.py` `tests/levels/level3/test_answer_relevancy.py` `tests/levels/level3/test_contradiction_handling.py`
- [ ] [PENDING] T8: Supersede old evals + docs — delete `tests/eval/test_search_recall.py` and `tests/eval/test_grounding_gate.py` and the empty `tests/eval/` dir; update README "Development" (test structure: document `tests/levels/` incl. DAG trajectory tier + calibrated judges, remove `tests/eval/` references, add per-level commands); verify full suite `uv sync --all-extras && uv run pytest` green (baseline + levels; real-LLM tiers skipped without key) + grep-verify no `input()`/`patch("builtins.input")` in `tests/levels/` `tests/eval/` `README.md`

## Interface handoffs
<!-- Populated by the orchestrator from each task's handoff contract block. Do not pre-fill beyond the stub. -->
- (none yet)
