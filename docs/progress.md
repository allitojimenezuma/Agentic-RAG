# Progress

<!-- One line per task. Status markers: PENDING | RUNNING | COMPLETED | BLOCKED -->

- [ ] [PENDING] T1: Build committed eval corpus `tests/fixtures/eval_wiki/` (~20 pages seeded from `wiki copy/`, cleaned so `health_check` = zero issues, AGENTS.md-conformant, entities/concepts/sources/comparisons + index.md + log.md) + `tests/fixtures/eval_corpus.py` (`EVAL_WIKI_SRC`, `CURATED_QUERIES` reusing the 15 pairs from the old `tests/eval/test_search_recall.py`, `HARD_QUERIES` ≥5 typo/synonym/cross-type variants, `RECALL_K=8`, thresholds 0.90/0.70, `copy_eval_wiki(tmp_path)`, `eval_wiki` fixture) + `tests/fixtures/eval_judge.py` (`judge_model()` from Settings, `FaithfulnessScore`/`RelevancyScore` pydantic models, `judge_faithfulness(question, answer, contexts)`, `judge_relevancy(question, answer)` — strict JSON + one retry) + `tests/levels/conftest.py` (re-export eval_wiki, skipif helper) + corpus self-check tests (fixture loads, health_check zero issues, every ground-truth slug exists) `tests/fixtures/eval_wiki/` `tests/fixtures/eval_corpus.py` `tests/fixtures/eval_judge.py` `tests/levels/conftest.py`
- [ ] [PENDING] T2: Level 1 deterministic suite — `tests/levels/level1/test_schema_conformance.py` (every corpus page: frontmatter required fields, slug↔filename match, type↔directory match, `## Related` present) + `test_path_guard_matrix.py` (exhaustive: all 7 write tools × {`raw/`, absolute, `..`, `./raw`, traversal `a/../b`} → ERROR string; `read_source` with raw path allowed; valid in-wiki slugs allowed) + `test_link_integrity.py` (health_check over corpus copy: no broken-link/orphan/missing-frontmatter/missing-index; every `[[link]]` target resolves via markdown_parser extraction) `tests/levels/level1/`
- [ ] [PENDING] T3: Level 2 deterministic tool-selection invariants — `tests/levels/level2/test_tool_selection.py` with `ScriptedChatModel` + `build_agent`: query agent only calls {wiki_search, wiki_read_page, wiki_summary} and never writes; ingest ordering `read_source` → `submit_extraction` (exactly once) → match decision → write → `regenerate_index` + `append_log` at end; lint calls `run_health_check` exactly once and before `write_lint_report`; fix respects the pinned kind→tool map (missing-frontmatter→add_frontmatter, broken-link→fix_link, missing-related→append_related_section, missing-index→regenerate_index, orphan/empty/stale→no fix tool) `tests/levels/level2/test_tool_selection.py`
- [ ] [PENDING] T4: Level 2 deterministic schemas + efficiency + state — `tests/levels/level2/test_argument_schemas.py` (invalid args return error strings never raise: create_page bad type/slug, flag_contradiction missing fields, fix_link bad targets, path-guard short-circuit shape) + `test_turn_efficiency.py` (happy-path tool-call caps: query ≤ 5, lint ≤ 4, fix ≤ 8, ingest ≤ 15) + `test_state_consistency.py` (post-ingest on tmp corpus copy: `regenerate_index` idempotent vs on-disk index.md, log.md appended, no orphan pages introduced, written pages valid frontmatter) `tests/levels/level2/test_argument_schemas.py` `tests/levels/level2/test_turn_efficiency.py` `tests/levels/level2/test_state_consistency.py`
- [ ] [PENDING] T5: Level 2 real-LLM trajectory tier (acceptance) — `tests/levels/level2/test_trajectory_real_llm.py`: `TRAJECTORY_TASKS` pinned ~8-task set (query single-hop + multi-hop, ingest happy path + contradiction, lint full check, fix missing-frontmatter/broken-link/missing-related) run via REAL builders (`build_*_agent(settings)`, pattern of `tests/acceptance/test_wiki_health.py`), trajectory from `result["messages"]`; per-task contract = expected tools in order (nav reads may interleave), no forbidden tools per agent, tool args parse vs pydantic schema, tool-call count ≤ caps; aggregate pass rate ≥ 0.8 with failing tasks printing full trajectory; reports tool-selection accuracy (correct-first-tool rate); `skipif` no key; never asserts model wording `tests/levels/level2/test_trajectory_real_llm.py`
- [ ] [PENDING] T6: Level 3 retrieval + deterministic faithfulness — `tests/levels/level3/test_context_recall.py` (recall@8 per query over CURATED_QUERIES and HARD_QUERIES on `eval_wiki` copy; aggregate thresholds 0.90/0.70; report actual numbers; fix corpus not thresholds) + `test_faithfulness.py` deterministic tier ALWAYS runs (every answer citation slug ∈ turn's NavCapture navigated set; every `[[link]]` in answer text ↔ citation consistency; no uncited links; build_final_answer confidence inference) `tests/levels/level3/test_context_recall.py` `tests/levels/level3/test_faithfulness.py`
- [ ] [PENDING] T7: Level 3 real-judge + contradiction handling — `test_faithfulness.py` judge tier (`skipif` no key; judge_faithfulness over scripted grounded vs fabricated answers on corpus contexts; score/JSON/pydantic round-trip proven with stub model) + `test_answer_relevancy.py` (deterministic proxy always runs: non-empty answer, length bounds, query key-terms present when appropriate; judge tier skipif: judge_relevancy on relevant vs off-topic answers) + `test_contradiction_handling.py` (scripted contradiction flow: submit_extraction with conflicting claim → `match_page` decision `conflict` → `flag_contradiction` called with page_slug/existing_claim/new_claim/proposed_resolution; HITL approve/reject/edit decision shapes; after edit-resolution approve → page updated with new claim + log entry + index consistent) `tests/levels/level3/test_faithfulness.py` `tests/levels/level3/test_answer_relevancy.py` `tests/levels/level3/test_contradiction_handling.py`
- [ ] [PENDING] T8: Supersede old evals + docs — delete `tests/eval/test_search_recall.py` and `tests/eval/test_grounding_gate.py` and the empty `tests/eval/` dir; update README "Development" (test structure: document `tests/levels/` incl. the real-LLM trajectory tier, remove `tests/eval/` references, add per-level commands); verify full suite `uv sync --all-extras && uv run pytest` green (baseline + levels; judge + trajectory tiers skipped without key) `tests/eval/` `README.md`

## Interface handoffs
<!-- Populated by the orchestrator from each task's handoff contract block. Do not pre-fill beyond the stub. -->
- (none yet)
