# Progress

<!-- One line per task. Status markers: PENDING | RUNNING | COMPLETED | BLOCKED -->

- [x] [COMPLETED] T1: Build `frontend/agent_driver.py` — multi-mode streaming driver for HITL agents (`InterruptEvent`, `FinalMessage`, `extract_interrupts`, `build_decisions`, `stream_turn`, `resume_turn`; reuses `ToolStart/ToolEnd/AnswerToken` from `chat_driver.py`) + `tests/unit/test_agent_driver.py` `frontend/agent_driver.py` `tests/unit/test_agent_driver.py`
- [x] [COMPLETED] T2: Build `frontend/history_store.py` — durable JSONL transcript store (`append/load/list_threads/delete/new_thread_id`) + `tests/unit/test_history_store.py`; add `frontend/history/` to `.gitignore` `frontend/history_store.py` `tests/unit/test_history_store.py` `.gitignore`
- [x] [COMPLETED] T3: Build `frontend/agents.py` (cached `get_*_agent` + `agent_config` mirroring cli.py per-agent config shapes) + `frontend/ui_common.py` (session init, sidebar thread manager, HITL action renderer + decision widgets, `run_turn` rerun shell) + `tests/unit/test_ui_common.py` (pure asserts + AppTest HITL flow) `frontend/agents.py` `frontend/ui_common.py` `tests/unit/test_ui_common.py`
- [x] [COMPLETED] T4: Restructure `frontend/app.py` into `st.navigation` entry (4 pages, `st.Page` paths relative to `frontend/`) + move query chat to `frontend/app_pages/query.py` (chat_driver + structured render + durable history via `HistoryStore`) + `tests/unit/test_app_smoke.py` (AppTest renders app.py + query page, no LLM) `frontend/app.py` `frontend/app_pages/query.py` `tests/unit/test_app_smoke.py`
- [ ] [PENDING] T5: Build `frontend/app_pages/ingest.py` — chat + raw/ file picker (`Ingest <relpath>` message) + full HITL (approve/reject/edit via `run_turn`) + `build_fix_message` helper in `agent_driver.py` with unit tests; extend `test_app_smoke.py`; update README "Web UI (Streamlit)" section `frontend/app_pages/ingest.py` `frontend/agent_driver.py` `tests/unit/test_agent_driver.py` `tests/unit/test_app_smoke.py` `README.md`
- [ ] [PENDING] T6: Build `frontend/app_pages/lint.py` (pinned health-check message + approve/reject HITL) + `frontend/app_pages/fix.py` (pills quick-actions + approve/reject HITL) + extend `test_app_smoke.py` `frontend/app_pages/lint.py` `frontend/app_pages/fix.py` `tests/unit/test_app_smoke.py`

## Interface handoffs
<!-- Populated by the orchestrator from each task's handoff contract block. Do not pre-fill beyond the stub. -->
- T1 exports: `InterruptEvent`, `FinalMessage`, `AgentEvent`, `ALLOWED_DECISIONS`, `extract_interrupts(state) -> list[dict]`, `build_decisions(choice, actions, *, feedback, index, new_resolution) -> list[dict]`, `stream_turn(agent, message, config, agent_name) -> AsyncGenerator[AgentEvent]`, `resume_turn(agent, decisions, config, agent_name) -> AsyncGenerator[AgentEvent]` in `frontend/agent_driver.py` (private: `_drive_turn`, `_state_values`, `_last_ai_text`; module constant `PAUSED_TOOL_OUTPUT`)
- T2 exports: `HistoryStore(root: Path)` + `DEFAULT_ROOT` (= `<repo>/frontend/history` derived from `__file__`) in `frontend/history_store.py`; callers use `HistoryStore(DEFAULT_ROOT)`. Methods: `append(agent, thread_id, role, content)` (one JSONL line `{"role","content"}`, mkdir parents, UTF-8), `load(agent, thread_id) -> list[dict]` (written order; missing -> `[]`; corrupt lines skipped; never raises), `list_threads(agent) -> list[str]` (mtime desc), `delete(agent, thread_id)` (unlink missing_ok, never raises), `new_thread_id() -> str` (str(uuid.uuid4())). Stdlib only. Agent dirs created lazily on first append. No path-traversal guard (single-user).
- T3 exports: `frontend/agents.py` — `@st.cache_resource` `get_settings()`, `get_query_agent()`, `get_ingest_agent()`, `get_lint_agent()`, `get_fix_agent()`; `agent_config(agent, thread_id) -> dict` (query -> `{"configurable": {"thread_id": tid}, "recursion_limit": settings.recursion_limit}`; ingest -> `settings.ingest_recursion_limit`; lint/fix -> NO recursion_limit key; mirrors cli.py).
- T3 exports: `frontend/ui_common.py` — `AGENTS: dict[str, str]` + per-agent Material icon; `init_page(agent) -> tuple[str, list]` (per-agent `f"{agent}_thread_id"` / `f"{agent}_messages"` session-state); `render_history(messages)`; `sidebar(agent, store: HistoryStore)` (thread selector, New chat, delete-selected); `render_actions(actions: list[dict])`; `run_turn(agent, agent_name, message, store)` (shared rerun shell: pending-resume path vs fresh-submit path; interrupt -> persist partial + `f"{agent}_pending"` marker + decision widgets: Approve all / Reject all+feedback / Edit resolution text_input for flag_contradiction only; FinalMessage -> render + persist + clear pending); `render_final(agent_name, final: FinalMessage) -> str`.
- T4: `frontend/app.py` is now the `st.navigation` entry point — bootstrap + Settings() guard + `st.set_page_config("Agentic RAG", wide)` + `st.navigation([...4 st.Page...], position="top").run()`; page list filtered by `.is_file()` so missing pages don't break nav (T5/T6 add ingest/lint/fix). `frontend/app_pages/query.py` hosts the query chat (stream_query + structured QueryAnswer render + reset_chat + _nav_capture contract via chat_driver; durable history via `HistoryStore` + ui_common `sidebar`/`init_page`; "New chat" affordance; 4-line repo-root bootstrap). `tests/unit/test_app_smoke.py` renders app.py + query.py via AppTest with fake key, no LLM.
